//import{ v4asuuidv4 }from 'uuid';

export interface CustomerSearchResponse {
    natId: string;
    customerId: string;
    accNumber: string;
    mobilePhone: string;
    firstName: string;
    lastName : string;
}