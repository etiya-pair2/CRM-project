import { UUIDShortenerPipe } from './uuidShortener.pipe';

describe('UUIDShortenerPipe', () => {
  it('create an instance', () => {
    const pipe = new UUIDShortenerPipe();
    expect(pipe).toBeTruthy();
  });
});
